[pytest]
pythonpath = src/efficientnet_lite_gpu
testpaths = tests
addopts =
    -q
    --cov=tools
    --cov-fail-under=90
    --cov-report=term-missing:skip-covered
filterwarnings =
    ignore:.*pkg_resources.*:DeprecationWarning
